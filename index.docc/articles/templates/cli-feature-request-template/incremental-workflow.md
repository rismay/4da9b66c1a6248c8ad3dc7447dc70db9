# Incremental workflow (single artifact)

@Metadata {
  @PageKind(article)
  @TitleHeading("Incremental workflow")
}

Describe how to build one canonical artifact incrementally (init → append → assign → mirror).

## Topics

- init
- append/update semantics (e.g. latest wins)
- assignment overwrite semantics
- mirror/post payload generation
